[package]
name = "error_sites"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
cordial = { path = "../../../../", default-features = false, features = ["error_sites"] }

[lib]
path = "src/lib.rs"
